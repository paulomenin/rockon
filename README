Rockon a XMMS2 client

This is a development version.

It's far, far away from release.

Don't be scared by the default theme! Now it's only for test.

-- DEPENDENCIES

 - xmms2
 - ecore
 - ecore-evas
 - ecore-file
 - edje

-- INSTALL

Rockon uses the Waf build system.
Just run:
$ ./waf configure build

If successful the executable will be at: build/default/rockon
